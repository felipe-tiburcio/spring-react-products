const Form = () => {
  return (
    <form action="">
      <input placeholder="Name" />
      <input placeholder="Brand" />

      <button>Save</button>
      <button>Edit</button>
      <button>Delete</button>
      <button>Cancel</button>
    </form>
  );
};

export default Form;
